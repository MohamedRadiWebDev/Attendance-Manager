## Packages
recharts | For the dashboard analytics charts
lucide-react | Already in base, but emphasizing need for specific icons
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
The application requires RTL layout support.
API endpoints handle file uploads via FormData.
Attendance calculation is an explicit action.
